// =============================================================================
// Convenience re-export shim. Domain types live in ../types/*.ts (split by area).
// Keep `import ... from "./types"` working for files that sit at src/ root.
// =============================================================================
export * from "./types/index"
