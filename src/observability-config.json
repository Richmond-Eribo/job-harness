{
  "$schema": "Trace capture config for the Agent Harness.",
  "$doc": "Non-secret observability knobs. Lives in version control (NOT in env) because these aren't credentials. The harness reads this on startup via static JSON import. Reasoning capture costs ZERO extra model tokens — the model produces the reasoning whether we persist it or not; we only pay SQLite storage.",

  "trace": {
    "captureReasoning": true,
    "maxReasoningChars": 8000,
    "maxTextChars": 16000,
    "captureWarnings": true,
    "captureProviderMetadata": false
  },

  "logging": {
    "redactToolArgs": ["apiKey", "token", "password", "authorization"],
    "maxToolOutputChars": 4000
  }
}
