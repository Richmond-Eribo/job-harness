# The `agents` SDK declares `ai@^6` as a peerOptional, but the root pins
# `ai@^7` (the AI SDK v7 streamText API the harness depends on). v7 is
# backwards-compatible for our usage. Use legacy peer-dep resolution so npm
# doesn't refuse the install over an OPTIONAL peer mismatch.
legacy-peer-deps=true
